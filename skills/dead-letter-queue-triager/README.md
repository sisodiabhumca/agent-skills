# Dead Letter Queue Triager

Reference implementation for `dead-letter-queue-triager`.

## Run

```bash
python run.py --input ../../samples/dead-letter-queue-triager/input.json --output ../../samples/dead-letter-queue-triager/output.json
```
