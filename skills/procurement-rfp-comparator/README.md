# Procurement RFP Comparator

Reference implementation for `procurement-rfp-comparator`.

## Run

```bash
python run.py --input ../../samples/procurement-rfp-comparator/input.json --output ../../samples/procurement-rfp-comparator/output.json
```
