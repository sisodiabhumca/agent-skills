# experiment-metric-audit

Vendor-neutral skill to audit an experiment's metric definitions for common analytics pitfalls.

## Run on the bundled sample

```bash
python experiment_metric_audit.py \
  --input ../../samples/experiment-metric-audit/experiment.json \
  --json-out /tmp/experiment_metric_audit.json
```
