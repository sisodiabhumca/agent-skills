# data-retention-policy-checker

Check a data retention schedule for common gaps and risky patterns.

## Run

```bash
python check_retention_policy.py \
  --input ../../samples/data-retention-policy-checker/policy.json \
  --output /tmp/retention_policy_report.md
```
