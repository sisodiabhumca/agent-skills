# Webhook Reliability Auditor

Reference implementation for `webhook-reliability-auditor`.

## Run

```bash
python run.py --input ../../samples/webhook-reliability-auditor/input.json --output ../../samples/webhook-reliability-auditor/output.json
```
