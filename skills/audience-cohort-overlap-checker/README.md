# Audience Cohort Overlap Checker

Reference implementation for `audience-cohort-overlap-checker`.

## Run

```bash
python run.py --input ../../samples/audience-cohort-overlap-checker/input.json --output ../../samples/audience-cohort-overlap-checker/output.json
```
