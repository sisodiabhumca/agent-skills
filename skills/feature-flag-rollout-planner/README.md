# feature-flag-rollout-planner

Generate a staged rollout plan for a feature flag.

## Run

```bash
python plan_rollout.py \
  --input ../../samples/feature-flag-rollout-planner/feature.json \
  --output /tmp/rollout_plan.md
```
