# docx-style-auditor

Vendor-neutral audit for style consistency in `.docx` documents (OpenXML).

## Run

```bash
python docx_style_auditor.py \
  --input ../../samples/docx-style-auditor/sample.docx \
  --out /tmp/docx_style_report.json
```
