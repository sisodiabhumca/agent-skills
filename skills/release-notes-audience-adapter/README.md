# Release Notes Audience Adapter

Reference implementation for `release-notes-audience-adapter`.

## Run

```bash
python run.py --input ../../samples/release-notes-audience-adapter/input.json --output ../../samples/release-notes-audience-adapter/output.json
```
