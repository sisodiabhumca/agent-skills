# SLA Report Consistency Checker

Reference implementation for `sla-report-consistency-checker`.

## Run

```bash
python run.py --input ../../samples/sla-report-consistency-checker/input.json --output ../../samples/sla-report-consistency-checker/output.json
```
