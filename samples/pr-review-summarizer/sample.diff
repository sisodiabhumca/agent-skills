diff --git a/src/auth/login.py b/src/auth/login.py
index 1111111..2222222 100644
--- a/src/auth/login.py
+++ b/src/auth/login.py
@@ -10,6 +10,12 @@ def login(user, password):
-    if check(user, password):
-        return token(user)
+    if check(user, password):
+        if not mfa_ok(user):
+            audit('mfa_required', user)
+            return None
+        token_value = token(user)
+        audit('login', user)
+        return token_value
     return None
diff --git a/migrations/20260507_add_mfa.sql b/migrations/20260507_add_mfa.sql
new file mode 100644
index 0000000..3333333
--- /dev/null
+++ b/migrations/20260507_add_mfa.sql
@@ -0,0 +1,4 @@
+ALTER TABLE users
+  ADD COLUMN mfa_enabled BOOLEAN NOT NULL DEFAULT false,
+  ADD COLUMN mfa_secret TEXT;
+CREATE INDEX idx_users_mfa ON users(mfa_enabled);
diff --git a/package.json b/package.json
index 4444444..5555555 100644
--- a/package.json
+++ b/package.json
@@ -20,6 +20,7 @@
     "express": "^4.19.0",
+    "speakeasy": "^2.0.0",
     "pg": "^8.11.0"
   }
 }
diff --git a/docs/auth.md b/docs/auth.md
index 6666666..7777777 100644
--- a/docs/auth.md
+++ b/docs/auth.md
@@ -1,3 +1,5 @@
 # Authentication
 
+MFA is now required for all admin users.
+
 The login flow exchanges credentials for a token.
